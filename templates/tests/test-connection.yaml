apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bus-reservation.fullname" . }}-test-connection"
  labels:
    {{- include "bus-reservation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bus-reservation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
